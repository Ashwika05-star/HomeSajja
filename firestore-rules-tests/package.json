{
  "name": "homesajja-firestore-rules-tests",
  "private": true,
  "description": "Runs ../firestore.rules against the local Firestore emulator (needs Node and JDK 21+).",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-homesajja \"node rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^15.30.2"
  }
}
