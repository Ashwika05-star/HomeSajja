{
  "name": "homesajja-functions",
  "description": "Optional: sends an FCM push for every new notification document. Needs the Firebase Blaze plan to deploy.",
  "private": true,
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  }
}
